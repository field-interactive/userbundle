{% extends 'base.html.twig' %}

{% block body %}
    <h1>Hello {{ user.name }}!</h1>
    <p>Your profile:</p>
    <p>Name: {{ user.name }}</p>
    <p>Email: {{ user.email }}</p>
    <a href="{{ path('profile_edit') }}">Edit</a>
{% endblock %}