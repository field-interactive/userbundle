{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Change Password" />
    {{ form_end(form) }}
    <ul>
        <li><a href="{{ path('profile_edit') }}">Back</a></li>
    </ul>
{% endblock %}