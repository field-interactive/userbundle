{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Register" />
    {{ form_end(form) }}
    <ul>
        <li><a href="{{ path('login') }}">Log in</a></li>
    </ul>
{% endblock %}