{% extends 'base.html.twig' %}

{% block body %}
    {% for user in users %}
        <ul>
            <li><a href="{{ path('admin_user_show', { 'id': user.id }) }}">{{ user.name }}</a></li>
        </ul>
    {% endfor %}
{% endblock %}