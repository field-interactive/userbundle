{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Edit" />
    {{ form_end(form) }}
    <a href="{{ path('profile_change_password') }}">Change Password</a>
    <a href="{{ path('profile_delete') }}">Delete Personal Data</a>
{% endblock %}